{
  "name": "filerouter-cli-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "example"
  ],
  "scripts": {
    "test": "bun test --cwd packages/filerouter-cli",
    "typecheck": "bunx tsc --noEmit -p packages/filerouter-cli/tsconfig.json",
    "build": "bun run --cwd packages/filerouter-cli build"
  }
}
